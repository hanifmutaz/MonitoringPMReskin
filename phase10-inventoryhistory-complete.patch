diff --git a/docs/frontend/COMPONENT-INVENTORY.md b/docs/frontend/COMPONENT-INVENTORY.md
index b6f4777..3a85f05 100644
--- a/docs/frontend/COMPONENT-INVENTORY.md
+++ b/docs/frontend/COMPONENT-INVENTORY.md
@@ -27,9 +27,9 @@ Status legend: **Existing** = present, keep as-is. **Refactor** = present, needs
 
 | Component | Status | Current location | Responsibility | Domain knowledge allowed | Major props | States | Pages using it | Migration notes |
 |---|---|---|---|---|---|---|---|---|
-| Pagination | **Done (Phase 5)** | `components/data-display/Pagination.jsx` (`components/Pagination.jsx` is now a re-export shim) | Page navigation for a list | None | `page, limit, total, onPageChange` (verified, unchanged) | — | `PmPartMonitoringPage`, `PmPartHistoryPage`, `PmLineHistoryPage`, `InventoryHistoryPage` (all still import the shim path) | Calculation logic untouched, only relocated. Page-level imports migrate to the direct path in Phase 6-10, not this phase. |
+| Pagination | **Done (Phase 5)** | `components/data-display/Pagination.jsx` (`components/Pagination.jsx` is now a re-export shim) | Page navigation for a list | None | `page, limit, total, onPageChange` (verified, unchanged) | — | Only `PmPartHistoryPage` still imports the shim path directly — `PmPartMonitoringPage`/`PmLineHistoryPage`/`InventoryHistoryPage` no longer call `<Pagination>` themselves at all, they render it indirectly through `DataTable` (which imports the canonical `./Pagination` path, confirmed via grep) since their Phase 7/8/10 migrations | Calculation logic untouched, only relocated. `PmPartHistoryPage` (not yet migrated to `DataTable`) is now the shim's sole remaining consumer. |
 | StatusBadge | **Done (Phase 5)** | `components/data-display/StatusBadge.jsx` (`components/StatusBadge.jsx` is now a re-export shim) | OK/Warning/Danger status pill | PM/Inventory status semantics — allowed domain knowledge, not a `ui/` primitive | `status` | OK/WARNING/DANGER/fallback | `DashboardPmLineWeeklyPage`, `PmPartMonitoringPage`, `PmLineStatusPage` (all still import the shim path) | The `components.css` `.badge-*` duplication named in the original audit is confirmed **already resolved** — a repo-wide grep at relocation time found zero live `className` usages of `.badge`/`.badge-*` (`badgeClassFor` no longer exists in `DashboardPage.jsx`). The dead CSS rules remain for Phase 14 cleanup. |
-| DataTable | **Wired into PmPartMonitoringPage (Phase 7), PmLineStatusPage/PmLineHistoryPage (Phase 8), LinesTab/PartsTab/SuppliersTab/InventoryTab×2/ClMappingModal/PartSupplierModal (Phase 9 — every Master Data table except the deliberately-excluded `ImportMasterDataTab.jsx`)** | `components/data-display/DataTable.jsx` | Presentational table: renders columns/rows against the verified envelope + loading/refreshing/empty/error states | None — receives columns/rows via props, all state copy caller-supplied | `columns, rows, getRowKey, isLoading, isRefreshing, isError, errorState, emptyState, page, limit, total, onPageChange, skeletonRows, selection` | loading (skeleton rows, accounts for the selection column too when present) / refreshing (dimmed, rows stay visible) / populated / empty / error — no-result is caller-composed via the exported `DataTableNoResult` helper, since only the page knows whether zero rows means "no data" or "filters matched nothing" | `selection` (added Phase 8, opt-in, shape matches `useRowSelection`'s return value) | Confirmed via 10 live consumers + clean `npm run build`/full-repo `eslint`. `page/limit/total/onPageChange` are all optional as a set — `PmLineStatusPage`/`ClMappingModal` omit them entirely (unpaginated data, shown all at once). `LinesTab`/`SuppliersTab` (Phase 9) are client-side paginated; `PartsTab`/`InventoryTab`'s item list are server-side with `SelectAllAcrossPagesBar` composed above, same pattern as Phase 7/8. `InventoryTab`'s movement-history table and `ClMappingModal`/`PartSupplierModal` (Phase 9) are the first consumers to use `DataTable` at its most minimal — just `columns`/`rows`/`isLoading`/`emptyState`, with or without `selection`, no pagination props at all — confirming the core loading/empty/populated states hold up standalone. `ImportMasterDataTab.jsx` was evaluated and deliberately **not** migrated — its per-row `include` checkbox is row data (via `updateRow`), not `useRowSelection`-style bulk-action selection, and it needs a sticky header `DataTable` doesn't support; forcing it into `selection` would be a category mismatch, not a fit — it remains the only hand-rolled table left in the Master Data domain. Sorting/column-filter callbacks still intentionally **not** included — still no page needs them. |
+| DataTable | **Wired into PmPartMonitoringPage (Phase 7), PmLineStatusPage/PmLineHistoryPage (Phase 8), LinesTab/PartsTab/SuppliersTab/InventoryTab×2/ClMappingModal/PartSupplierModal (Phase 9), InventoryHistoryPage (Phase 10)** | `components/data-display/DataTable.jsx` | Presentational table: renders columns/rows against the verified envelope + loading/refreshing/empty/error states | None — receives columns/rows via props, all state copy caller-supplied | `columns, rows, getRowKey, isLoading, isRefreshing, isError, errorState, emptyState, page, limit, total, onPageChange, skeletonRows, selection` | loading (skeleton rows, accounts for the selection column too when present) / refreshing (dimmed, rows stay visible) / populated / empty / error — no-result is caller-composed via the exported `DataTableNoResult` helper, since only the page knows whether zero rows means "no data" or "filters matched nothing" | `selection` (added Phase 8, opt-in, shape matches `useRowSelection`'s return value) | Confirmed via 11 live consumers + clean `npm run build`/full-repo `eslint`. `InventoryHistoryPage` (Phase 10) is the last of Master Data/Inventory's server-side-paginated + `selection` + `SelectAllAcrossPagesBar` tables — same pattern as `PartsTab`/`PmLineHistoryPage`, no new capability needed. With this, **every remaining hand-rolled table with real `useRowSelection` bulk-delete in the app has been migrated** — anything still hand-rolled from here on (`ImportMasterDataTab.jsx`, whatever's left in Phase 11) either has no selection need or was evaluated and excluded for a genuine shape mismatch. Sorting/column-filter callbacks still intentionally **not** included — still no page needs them. |
 | FilterBar | **Built, not yet wired (Phase 5)** | `components/data-display/FilterBar.jsx` | Layout wrapper for filter controls above a table (wrap + gap + actions-push-right) | None | `children, actions, className` | — | None yet | Deliberately thin — generalises exactly the `flex flex-wrap items-center gap-3` + `ml-auto` pattern already working in `PmPartMonitoringPage.jsx`'s filter row. Does not know about search/select/pills; those stay as the existing domain components composed as children. |
 | ColumnControl | New | none | Show/hide table columns | None | `Unknown` | — | None yet — only build if a real page needs it; brief lists it as a capability, not confirmed as currently used anywhere. |
 | ActivityList | New | `Unknown` — may overlap with `CriticalAlertsPanel.jsx`/`NotificationBell.jsx` | Recent activity feed | None | `Unknown` | — | Check `CriticalAlertsPanel.jsx` and `NotificationBell.jsx` before building — may already partially exist under different names. |
diff --git a/docs/frontend/MIGRATION-PLAN.md b/docs/frontend/MIGRATION-PLAN.md
index 70fe6e9..eef885c 100644
--- a/docs/frontend/MIGRATION-PLAN.md
+++ b/docs/frontend/MIGRATION-PLAN.md
@@ -107,7 +107,7 @@ A repo-wide grep (`className=` tokens, stripped of Tailwind arbitrary-value brac
 - **Scope**: `InventoryPage`, `InventoryHistoryPage`.
 - **Risks**: the confirmed snake_case field leak in `GET /inventory/rop-status` must be normalised at the adapter boundary here, per `FRONTEND-ARCHITECTURE.md`.
 - **Acceptance criteria**: `PackageRoute` upsell-in-place behavior for Package A instances preserved exactly.
-- **Status (updated 25 Aug 2026)**: `InventoryPage.jsx`'s `DataTable` work is **done** — it has no `<table>` of its own, it delegates entirely to `components/masterdata/InventoryTab.jsx`, which was migrated as part of Phase 9 (see Phase 9's status note; both of `InventoryTab.jsx`'s tables — item list and movement history — now use `DataTable`). This confirms the cross-phase link flagged earlier: migrating `InventoryTab.jsx` once closed the `DataTable` work for both Phase 9 (`MasterDataPage`) and Phase 10 (`InventoryPage`) simultaneously. `InventoryHistoryPage.jsx` has its own separate `<table>` (not shared with `InventoryTab.jsx`) — reskinned (batch 5/N) but **not yet migrated to `DataTable`**, still open.
+- **Status (DONE, 25 Aug 2026)**: both pages closed. `InventoryPage.jsx`'s `DataTable` work was closed by Phase 9 — it has no `<table>` of its own, it delegates entirely to `components/masterdata/InventoryTab.jsx` (both of its tables now use `DataTable`). This confirms the cross-phase link flagged earlier: migrating `InventoryTab.jsx` once closed the `DataTable` work for both Phase 9 (`MasterDataPage`) and Phase 10 (`InventoryPage`) simultaneously. `InventoryHistoryPage.jsx` had its own separate `<table>` (not shared with `InventoryTab.jsx`) — migrated to `DataTable` with `selection` + `SelectAllAcrossPagesBar`, same server-side-paginated pattern as `PartsTab`/`PmLineHistoryPage`. Columns (6) extracted to `pages/inventoryHistoryColumns.jsx` — co-located in `pages/` rather than a domain folder, since this page is the only consumer (same reasoning as `ClMappingModal.jsx` keeping its columns inline in Phase 9). `npm run build` + `eslint` clean. With this, the `components/Pagination.jsx` re-export shim (Phase 5) is down to a single remaining consumer, `PmPartHistoryPage` — every other page that used to import it directly now gets `Pagination` indirectly through `DataTable`.
 
 ## Phase 11 — Migrate Administration
 - **Scope**: `UserManagementPage`, `SettingsPage`, `RecycleBinPage`, plus wherever Roles & Permissions is found to live (per `OPEN-QUESTIONS.md`).
diff --git a/frontend/src/pages/InventoryHistoryPage.jsx b/frontend/src/pages/InventoryHistoryPage.jsx
index 9e07076..fbfb3b3 100644
--- a/frontend/src/pages/InventoryHistoryPage.jsx
+++ b/frontend/src/pages/InventoryHistoryPage.jsx
@@ -9,7 +9,18 @@
 // token bg-ok-dim/text-ok, bg-danger-dim/text-danger, bg-warn-dim/text-warn
 // yang sama dipakai RopBadge di InventoryTab. Query/pagination/logic TIDAK
 // berubah sama sekali.
+//
+// DataTable migration (docs/frontend/MIGRATION-PLAN.md Phase 10): hand-
+// rolled <table> diganti data-display/DataTable dengan `selection` +
+// `SelectAllAcrossPagesBar` - pola identik PartsTab.jsx/PmLineHistoryPage.jsx
+// (server-side paginated, sudah punya bulk-delete via useRowSelection).
+// Kolom (6) pindah ke inventoryHistoryColumns.jsx, co-located di pages/
+// (bukan folder domain terpisah - halaman ini satu-satunya consumer,
+// gak ada komponen lain yang perlu digabung ke domain folder baru,
+// sama alasan ClMappingModal.jsx tetap mendefinisikan kolomnya sendiri).
+// Semua state/query/pagination/handler TIDAK berubah.
 import { useState } from 'react';
+import { Inbox } from 'lucide-react';
 import { usePageHeader } from '../contexts/PageHeaderContext';
 import { useAllInventoryMovements } from '../hooks/useInventoryItemDetail';
 import { useInventoryItems } from '../hooks/useInventoryItems';
@@ -17,25 +28,15 @@ import { useRowSelection } from '../hooks/useRowSelection';
 import { useBulkDeleteMutation } from '../hooks/useRecycleBin';
 import { useConfirm } from '../contexts/ConfirmDialogContext';
 import { fetchAllInventoryMovements } from '../api/inventoryApi';
-import Pagination from '../components/Pagination';
 import BulkDeleteBar from '../components/BulkDeleteBar';
 import SelectAllAcrossPagesBar from '../components/SelectAllAcrossPagesBar';
+import { DataTable, DataTableNoResult } from '../components/data-display/DataTable';
+import { EmptyState } from '../components/ui/empty-state';
 import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../components/ui/select';
+import inventoryHistoryColumns, { MOVEMENT_TYPE_LABEL } from './inventoryHistoryColumns';
 
 const LIMIT = 20;
 
-const MOVEMENT_TYPE_LABEL = {
-    STOCK_IN: 'Stock In',
-    STOCK_OUT: 'Stock Out',
-    ADJUSTMENT: 'Adjustment',
-};
-
-const MOVEMENT_TYPE_BADGE_CLASS = {
-    STOCK_IN: 'bg-ok-dim text-ok',
-    STOCK_OUT: 'bg-danger-dim text-danger',
-    ADJUSTMENT: 'bg-warn-dim text-warn',
-};
-
 function InventoryHistoryPage() {
     const [itemId, setItemId] = useState('all');
     const [movementType, setMovementType] = useState('all');
@@ -56,7 +57,7 @@ function InventoryHistoryPage() {
         page,
         limit: LIMIT,
     };
-    const { data, isLoading, isError } = useAllInventoryMovements(params);
+    const { data, isLoading, isFetching, isError } = useAllInventoryMovements(params);
     const pageIds = data?.items?.map((m) => m.id) ?? [];
     const selection = useRowSelection(pageIds);
     // 'inventory-movements' - murni log historis, TIDAK ngaruh ke
@@ -87,6 +88,14 @@ function InventoryHistoryPage() {
         }
     }
 
+    const hasActiveFilter = itemId !== 'all' || movementType !== 'all';
+
+    function handleResetFilter() {
+        setItemId('all');
+        setMovementType('all');
+        setPage(1);
+    }
+
     return (
         <div className="flex flex-col gap-4">
             <div className="flex flex-wrap gap-2">
@@ -131,117 +140,49 @@ function InventoryHistoryPage() {
                 </Select>
             </div>
 
-            <div className="rounded-lg border border-border bg-card p-4.5">
-                {isError && (
-                    <div className="rounded-lg bg-[var(--danger-dim)] px-3 py-2 text-xs text-[var(--danger)]">
-                        Gagal memuat riwayat. Coba lagi.
-                    </div>
-                )}
-
-                {bulkError && (
-                    <div className="mb-3 rounded-lg bg-[var(--danger-dim)] px-3 py-2 text-xs text-[var(--danger)]">
-                        {bulkError}
-                    </div>
-                )}
-
-                <BulkDeleteBar
-                    count={selection.selectedCount}
-                    onDelete={handleBulkDelete}
-                    onClear={selection.clear}
-                    pending={bulkDelete.isPending}
-                    label="Riwayat"
+            {bulkError && (
+                <div className="rounded-lg bg-[var(--danger-dim)] px-3 py-2 text-xs text-[var(--danger)]">
+                    {bulkError}
+                </div>
+            )}
+
+            <BulkDeleteBar
+                count={selection.selectedCount}
+                onDelete={handleBulkDelete}
+                onClear={selection.clear}
+                pending={bulkDelete.isPending}
+                label="Riwayat"
+            />
+
+            {data && selection.allOnPageSelected && (
+                <SelectAllAcrossPagesBar
+                    pageCount={pageIds.length}
+                    total={data.total}
+                    alreadySelectedAll={selection.selectedCount >= data.total}
+                    onSelectAll={handleSelectAllMatching}
                 />
-
-                {data && selection.allOnPageSelected && (
-                    <SelectAllAcrossPagesBar
-                        pageCount={pageIds.length}
-                        total={data.total}
-                        alreadySelectedAll={selection.selectedCount >= data.total}
-                        onSelectAll={handleSelectAllMatching}
-                    />
-                )}
-
-                {isLoading && !data && (
-                    <div className="py-8 text-center text-sm text-[var(--text-faint)]">Memuat data...</div>
-                )}
-                {data && data.items.length === 0 && (
-                    <div className="py-8 text-center text-sm text-[var(--text-faint)]">Belum ada mutasi stok.</div>
-                )}
-
-                {data && data.items.length > 0 && (
-                    <>
-                        <div className="overflow-hidden rounded-lg border border-border">
-                            <div className="overflow-x-auto">
-                                <table className="w-full border-collapse">
-                                    <thead>
-                                        <tr className="border-b border-border">
-                                            <th className="w-[36px] px-3 py-2">
-                                                <input
-                                                    type="checkbox"
-                                                    checked={selection.allOnPageSelected}
-                                                    ref={(el) => {
-                                                        if (el)
-                                                            el.indeterminate =
-                                                                selection.someOnPageSelected && !selection.allOnPageSelected;
-                                                    }}
-                                                    onChange={selection.toggleAllOnPage}
-                                                    className="h-3.5 w-3.5 accent-[var(--accent)]"
-                                                />
-                                            </th>
-                                            {['Tanggal', 'Item', 'Jenis', 'Qty', 'Catatan', 'Oleh'].map((h) => (
-                                                <th
-                                                    key={h}
-                                                    className="whitespace-nowrap px-3 py-2 text-left font-[var(--font-mono)] text-[11px] uppercase tracking-[0.5px] text-[var(--text-faint)]"
-                                                >
-                                                    {h}
-                                                </th>
-                                            ))}
-                                        </tr>
-                                    </thead>
-                                    <tbody>
-                                        {data.items.map((m) => (
-                                            <tr key={m.id} className="border-b border-[var(--border-soft)] last:border-b-0 hover:bg-secondary">
-                                                <td className="px-3 py-3">
-                                                    <input
-                                                        type="checkbox"
-                                                        checked={selection.isSelected(m.id)}
-                                                        onChange={() => selection.toggle(m.id)}
-                                                        className="h-3.5 w-3.5 accent-[var(--accent)]"
-                                                    />
-                                                </td>
-                                                <td className="px-3 py-3 font-[var(--font-mono)] text-xs text-[var(--text-dim)]">
-                                                    {new Date(m.created_at).toLocaleString('id-ID')}
-                                                </td>
-                                                <td className="px-3 py-3">
-                                                    <div className="font-[var(--font-mono)] text-[13px]">{m.part_name}</div>
-                                                    <div className="text-xs text-[var(--text-dim)]">{m.spare_part_number}</div>
-                                                </td>
-                                                <td className="px-3 py-3">
-                                                    <span
-                                                        className={`rounded px-1.5 py-0.5 text-[11px] font-medium ${
-                                                            MOVEMENT_TYPE_BADGE_CLASS[m.movement_type] || 'bg-[var(--panel-3)] text-[var(--text-faint)]'
-                                                        }`}
-                                                    >
-                                                        {MOVEMENT_TYPE_LABEL[m.movement_type] || m.movement_type}
-                                                    </span>
-                                                </td>
-                                                <td className="px-3 py-3 font-[var(--font-mono)] text-[13px]">
-                                                    {m.movement_type === 'STOCK_OUT' ? '-' : '+'}
-                                                    {Number(m.qty).toLocaleString('id-ID')}
-                                                </td>
-                                                <td className="max-w-[240px] px-3 py-3 text-xs text-[var(--text-dim)]">{m.note || '-'}</td>
-                                                <td className="px-3 py-3 text-xs text-[var(--text-dim)]">{m.user_full_name}</td>
-                                            </tr>
-                                        ))}
-                                    </tbody>
-                                </table>
-                            </div>
-                        </div>
-
-                        <Pagination page={data.page} limit={data.limit} total={data.total} onPageChange={setPage} />
-                    </>
-                )}
-            </div>
+            )}
+
+            <DataTable
+                columns={inventoryHistoryColumns}
+                rows={data?.items}
+                getRowKey={(m) => m.id}
+                isLoading={isLoading && !data}
+                isRefreshing={isFetching && !isLoading}
+                isError={isError}
+                page={data?.page}
+                limit={data?.limit}
+                total={data?.total}
+                onPageChange={setPage}
+                selection={selection}
+                emptyState={
+                    hasActiveFilter ? (
+                        <DataTableNoResult onReset={handleResetFilter} />
+                    ) : (
+                        <EmptyState icon={Inbox} title="Belum ada mutasi stok" />
+                    )
+                }
+            />
         </div>
     );
 }
diff --git a/frontend/src/pages/inventoryHistoryColumns.jsx b/frontend/src/pages/inventoryHistoryColumns.jsx
new file mode 100644
index 0000000..5779e38
--- /dev/null
+++ b/frontend/src/pages/inventoryHistoryColumns.jsx
@@ -0,0 +1,75 @@
+// src/pages/inventoryHistoryColumns.jsx
+// New (docs/frontend/MIGRATION-PLAN.md Phase 10). Extracted from the
+// hand-rolled <table> in InventoryHistoryPage.jsx - same 6 columns,
+// following the pm-line/pm-part/masterdata columns-file precedent.
+// MOVEMENT_TYPE_LABEL/MOVEMENT_TYPE_BADGE_CLASS exported alongside since
+// the page's own <Select> filter needs MOVEMENT_TYPE_LABEL too (same
+// pattern as pmLineHistoryColumns.jsx exporting JENIS_LABEL).
+const MOVEMENT_TYPE_LABEL = {
+  STOCK_IN: 'Stock In',
+  STOCK_OUT: 'Stock Out',
+  ADJUSTMENT: 'Adjustment',
+};
+
+const MOVEMENT_TYPE_BADGE_CLASS = {
+  STOCK_IN: 'bg-ok-dim text-ok',
+  STOCK_OUT: 'bg-danger-dim text-danger',
+  ADJUSTMENT: 'bg-warn-dim text-warn',
+};
+
+const inventoryHistoryColumns = [
+  {
+    key: 'created_at',
+    header: 'Tanggal',
+    className: 'font-[var(--font-mono)] text-xs text-[var(--text-dim)]',
+    render: (m) => new Date(m.created_at).toLocaleString('id-ID'),
+  },
+  {
+    key: 'item',
+    header: 'Item',
+    render: (m) => (
+      <>
+        <div className="font-[var(--font-mono)] text-[13px]">{m.part_name}</div>
+        <div className="text-xs text-[var(--text-dim)]">{m.spare_part_number}</div>
+      </>
+    ),
+  },
+  {
+    key: 'movement_type',
+    header: 'Jenis',
+    render: (m) => (
+      <span
+        className={`rounded px-1.5 py-0.5 text-[11px] font-medium ${
+          MOVEMENT_TYPE_BADGE_CLASS[m.movement_type] || 'bg-[var(--panel-3)] text-[var(--text-faint)]'
+        }`}
+      >
+        {MOVEMENT_TYPE_LABEL[m.movement_type] || m.movement_type}
+      </span>
+    ),
+  },
+  {
+    key: 'qty',
+    header: 'Qty',
+    render: (m) => (
+      <span className="font-[var(--font-mono)] text-[13px]">
+        {m.movement_type === 'STOCK_OUT' ? '-' : '+'}
+        {Number(m.qty).toLocaleString('id-ID')}
+      </span>
+    ),
+  },
+  {
+    key: 'note',
+    header: 'Catatan',
+    className: 'max-w-[240px] text-xs text-[var(--text-dim)]',
+    render: (m) => m.note || '-',
+  },
+  {
+    key: 'user_full_name',
+    header: 'Oleh',
+    className: 'text-xs text-[var(--text-dim)]',
+    render: (m) => m.user_full_name,
+  },
+];
+
+export { MOVEMENT_TYPE_LABEL };
+export default inventoryHistoryColumns;
